import { MongoClient } from 'mongodb';
import { } from 'express';
import { } from 'body-parser';
import { } from 'amqplib/callback_api';
import { os } from 'os';

while (true) {
   console.log(os.name);
}